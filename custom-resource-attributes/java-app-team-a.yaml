# -----------------------------------------------------------------------------
# NOTE: This deployment is an example for illustration purposes.
# Please ensure you use the correct application image, configuration parameters,
# and adjust resource requests/limits appropriately for your actual environment.
# -----------------------------------------------------------------------------

apiVersion: apps/v1
kind: Deployment
metadata:
  name: java-app-team-a
  namespace: otel-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: java-app-team-a
  template:
    metadata:
      labels:
        app: java-app-team-a
    spec:
      containers:
        - name: java-app
          image: ghcr.io/open-telemetry/opentelemetry-demo:1.2.0-adservice # Sample Java app
          imagePullPolicy: IfNotPresent
          env:
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://otel-collector-custom:4317"
            # Adding custom resource attributes via environment variable
            - name: OTEL_RESOURCE_ATTRIBUTES
              value: "broadcom_organization=Broadcom,broadcom_division=IMS,broadcom_github_repo=sample-repo-alpha,broadcom_valuestream=ITOM,broadcom_user_id=demo-user-01,service.name=java-app-team-a"
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: java-app-team-a
  namespace: otel-demo
spec:
  selector:
    app: java-app-team-a
  ports:
    - port: 8080
      targetPort: 8080
